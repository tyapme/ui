---
title: shadcn
description: shadcn CLI を使ってプロジェクトにコンポーネントを追加します。
---

import { TriangleAlertIcon } from "lucide-react"

## init

`init` コマンドを使って、既存プロジェクトの設定と依存関係を初期化します。`--name` オプションを使うと新規プロジェクトの作成もできます。

`init` コマンドは依存関係のインストール、`cn` ユーティリティの追加、プロジェクトの CSS 変数の設定を行います。

```bash
npx shadcn@latest init
```

**オプション**

```bash
Usage: shadcn init [options] [components...]

initialize your project and install dependencies

Arguments:
  components                 names, url or local path to component

Options:
  -t, --template <template>  the template to use. (next, vite, start, react-router, laravel, astro)
  -b, --base <base>          the component library to use. (radix, base)
  -p, --preset [name]        use a preset configuration
  -y, --yes                  skip confirmation prompt. (default: true)
  -d, --defaults             use default configuration: --template=next --preset=nova (default: false)
  -f, --force                force overwrite of existing configuration. (default: false)
  -c, --cwd <cwd>            the working directory. defaults to the current directory.
  -n, --name <name>          the name for the new project.
  -s, --silent               mute output. (default: false)
  --css-variables            use css variables for theming. (default: true)
  --no-css-variables         do not use css variables for theming.
  --monorepo                 scaffold a monorepo project.
  --no-monorepo              skip the monorepo prompt.
  --rtl                      enable RTL support.
  --no-rtl                   disable RTL support.
  --pointer                  enable pointer cursor for buttons.
  --no-pointer               disable pointer cursor for buttons.
  --reinstall                re-install existing UI components.
  --no-reinstall             do not re-install existing UI components.
  -h, --help                 display help for command
```

`create` コマンドは `init` のエイリアスです：

```bash
npx shadcn@latest create
```

---

## add

`add` コマンドを使って、プロジェクトにコンポーネントと依存関係を追加します。

```bash
npx shadcn@latest add [component]
```

**オプション**

```bash
Usage: shadcn add [options] [components...]

add a component to your project

Arguments:
  components           name, url or local path to component

Options:
  -y, --yes            skip confirmation prompt. (default: false)
  -o, --overwrite      overwrite existing files. (default: false)
  -c, --cwd <cwd>      the working directory. defaults to the current directory.
  -a, --all            add all available components (default: false)
  -p, --path <path>    the path to add the component to.
  -s, --silent         mute output. (default: false)
  --dry-run            preview changes without writing files. (default: false)
  --diff [path]        show diff for a file.
  --view [path]        show file contents.
  -h, --help           display help for command
```

---

## apply

`apply` コマンドを使って、既存プロジェクトにプリセットを適用します。

```bash
npx shadcn@latest apply a2r6bw
```

UI コンポーネントを再インストールせず、テーマまたはフォントのみを適用することもできます：

```bash
npx shadcn@latest apply a2r6bw --only theme
```

`--only` に指定できる値は `theme` と `font` です。

**オプション**

```bash
Usage: shadcn apply [options] [preset]

apply a preset to an existing project

Arguments:
  preset             the preset to apply

Options:
  --preset <preset>  preset configuration to apply
  --only [parts]     apply only parts of a preset: theme, font
  -y, --yes          skip confirmation prompt. (default: false)
  -c, --cwd <cwd>    the working directory. defaults to the current directory.
  -s, --silent       mute output. (default: false)
  -h, --help         display help for command
```

---

## preset

`preset` コマンドを使って、プリセットコードを調べたり、既存プロジェクトのプリセットを解決したりします。

```bash
npx shadcn@latest preset decode a2r6bw
```

### preset decode

`preset decode` を使ってプリセットコードをデコードします。

```bash
npx shadcn@latest preset decode a2r6bw
```

**オプション**

```bash
Usage: shadcn preset decode [options] <code>

decode a preset code

Arguments:
  code        the preset code to decode

Options:
  --json      output as JSON. (default: false)
  -h, --help  display help for command
```

### preset resolve

`preset resolve` を使って現在のプロジェクトのプリセットを解決します。

```bash
npx shadcn@latest preset resolve
```

`preset info` コマンドは `preset resolve` のエイリアスです：

```bash
npx shadcn@latest preset info
```

**オプション**

```bash
Usage: shadcn preset resolve|info [options]

resolve a preset from your project

Options:
  -c, --cwd <cwd>  the working directory. defaults to the current directory.
  --json            output as JSON. (default: false)
  -h, --help        display help for command
```

### preset url

`preset url` を使ってプリセットコードの作成 URL を表示します。

```bash
npx shadcn@latest preset url a2r6bw
```

**オプション**

```bash
Usage: shadcn preset url [options] <code>

get the create URL for a preset code

Arguments:
  code        the preset code

Options:
  -h, --help  display help for command
```

### preset open

`preset open` を使ってプリセットコードをブラウザで開きます。

```bash
npx shadcn@latest preset open a2r6bw
```

**オプション**

```bash
Usage: shadcn preset open [options] <code>

open a preset code in the browser

Arguments:
  code        the preset code

Options:
  -h, --help  display help for command
```

---

## view

`view` コマンドを使って、インストール前にレジストリのアイテムを確認します。

```bash
npx shadcn@latest view [item]
```

複数のアイテムを一度に確認できます：

```bash
npx shadcn@latest view button card dialog
```

名前空間付きレジストリのアイテムを確認することもできます：

```bash
npx shadcn@latest view @acme/auth @v0/dashboard
```

**オプション**

```bash
Usage: shadcn view [options] <items...>

view items from the registry

Arguments:
  items            the item names or URLs to view

Options:
  -c, --cwd <cwd>  the working directory. defaults to the current directory.
  -h, --help       display help for command
```

---

## search

`search` コマンドを使って、レジストリからアイテムを検索します。

```bash
npx shadcn@latest search [registry]
```

クエリを指定して検索できます：

```bash
npx shadcn@latest search @shadcn -q "button"
```

複数のレジストリを一度に検索することもできます：

```bash
npx shadcn@latest search @shadcn @v0 @acme
```

`list` コマンドは `search` のエイリアスです：

```bash
npx shadcn@latest list @acme
```

**オプション**

```bash
Usage: shadcn search|list [options] <registries...>

search items from registries

Arguments:
  registries             the registry names or urls to search items from. Names
                         must be prefixed with @.

Options:
  -c, --cwd <cwd>        the working directory. defaults to the current directory.
  -q, --query <query>    query string
  -l, --limit <number>   maximum number of items to display per registry (default: "100")
  -o, --offset <number>  number of items to skip (default: "0")
  -h, --help             display help for command
```

---

## build

`build` コマンドを使って、レジストリの JSON ファイルを生成します。

```bash
npx shadcn@latest build
```

このコマンドは `registry.json` ファイルを読み込み、`public/r` ディレクトリにレジストリ JSON ファイルを生成します。

**オプション**

```bash
Usage: shadcn build [options] [registry]

build components for a shadcn registry

Arguments:
  registry             path to registry.json file (default: "./registry.json")

Options:
  -o, --output <path>  destination directory for json files (default: "./public/r")
  -c, --cwd <cwd>      the working directory. defaults to the current directory.
  -h, --help           display help for command
```

出力ディレクトリをカスタマイズするには `--output` オプションを使います。

```bash
npx shadcn@latest build --output ./public/registry
```

---

## docs

`docs` コマンドを使って、コンポーネントのドキュメントと API リファレンスを取得します。

```bash
npx shadcn@latest docs [component]
```

**オプション**

```bash
Usage: shadcn docs [options] [component]

fetch documentation and API references for components

Arguments:
  component          the component to get docs for

Options:
  -c, --cwd <cwd>    the working directory. defaults to the current directory.
  -b, --base <base>  the base to use either 'base' or 'radix'. defaults to project base.
  --json             output as JSON. (default: false)
  -h, --help         display help for command
```

---

## info

`info` コマンドを使って、プロジェクトの情報を取得します。

```bash
npx shadcn@latest info
```

**オプション**

```bash
Usage: shadcn info [options]

get information about your project

Options:
  -c, --cwd <cwd>  the working directory. defaults to the current directory.
  --json            output as JSON. (default: false)
  -h, --help        display help for command
```

---

## migrate

`migrate` コマンドを使って、プロジェクトのマイグレーションを実行します。

```bash
npx shadcn@latest migrate [migration]
```

**利用可能なマイグレーション**

| マイグレーション | 説明                                                         |
| ---------------- | ------------------------------------------------------------ |
| `icons`          | UI コンポーネントを別のアイコンライブラリに移行します。      |
| `radix`          | radix-ui に移行します。                                      |
| `rtl`            | コンポーネントを RTL（右から左）サポートに対応させます。     |

**オプション**

```bash
Usage: shadcn migrate [options] [migration] [path]

run a migration.

Arguments:
  migration        the migration to run.
  path             optional path or glob pattern to migrate.

Options:
  -c, --cwd <cwd>  the working directory. defaults to the current directory.
  -l, --list       list all migrations. (default: false)
  -y, --yes        skip confirmation prompt. (default: false)
  -h, --help       display help for command
```

---

### migrate rtl

`rtl` マイグレーションは、コンポーネントを RTL（右から左）言語に対応させます。

```bash
npx shadcn@latest migrate rtl
```

このコマンドは以下を行います：

1. `components.json` を更新して `rtl: true` を設定する
2. 物理的な CSS プロパティを論理的な等価物に変換する（例：`ml-4` → `ms-4`、`text-left` → `text-start`）
3. 必要な箇所に `rtl:` バリアントを追加する（例：`space-x-4` → `space-x-4 rtl:space-x-reverse`）

**特定ファイルのマイグレーション**

特定のファイルや glob パターンを使ってマイグレーションできます：

```bash
# 特定ファイルをマイグレーション
npx shadcn@latest migrate rtl src/components/ui/button.tsx

# glob パターンに一致するファイルをマイグレーション
npx shadcn@latest migrate rtl "src/components/ui/**"
```

パスを指定しない場合、`components.json` の `ui` ディレクトリ内のすべてのファイルが変換されます。

---

### migrate radix

`radix` マイグレーションは、個別の `@radix-ui/react-*` パッケージから統合された `radix-ui` パッケージへのインポートを更新します。

```bash
npx shadcn@latest migrate radix
```

このコマンドは以下を行います：

1. `@radix-ui/react-*` から `radix-ui` へのインポートを変換する
2. `radix-ui` パッケージを `package.json` に追加する

**変換前**

```tsx
import * as DialogPrimitive from "@radix-ui/react-dialog"
import * as SelectPrimitive from "@radix-ui/react-select"
```

**変換後**

```tsx
import { Dialog as DialogPrimitive, Select as SelectPrimitive } from "radix-ui"
```

**特定ファイルのマイグレーション**

特定のファイルや glob パターンを使ってマイグレーションできます：

```bash
# 特定ファイルをマイグレーション
npx shadcn@latest migrate radix src/components/ui/dialog.tsx

# glob パターンに一致するファイルをマイグレーション
npx shadcn@latest migrate radix "src/components/ui/**"
```

パスを指定しない場合、`components.json` の `ui` ディレクトリ内のすべてのファイルが変換されます。

完了後、`package.json` から不要になった `@radix-ui/react-*` パッケージを削除できます。

---

## eject

`init` を実行すると、shadcn はグローバル CSS ファイルに `@import "shadcn/tailwind.css"` を追加します。このインポートは、カスタムバリアント（`data-open:`、`data-closed:` など）やアコーディオンアニメーションなど、共有 Tailwind v4 ユーティリティを提供します。

`eject` コマンドを使って、`shadcn/tailwind.css` をグローバル CSS ファイルにインライン展開し、プロジェクトから `shadcn` 依存関係を削除します。

<Callout icon={<TriangleAlertIcon />}>
  **注意：この操作は元に戻せません。** eject 後、`shadcn/tailwind.css` への将来の shadcn CLI アップデートは自動的に適用されなくなります。
</Callout>

```bash
npx shadcn@latest eject
```

**変換前**

```css
@import "tailwindcss";
@import "tw-animate-css";
@import "shadcn/tailwind.css";
```

**変換後**

```css
@import "tailwindcss";
@import "tw-animate-css";
/* ejected from shadcn@4.8.3 */
@theme inline {
  @keyframes accordion-down {
    from {
      height: 0;
    }
    to {
      height: var(
        --radix-accordion-content-height,
        var(--accordion-panel-height, auto)
      );
    }
  }
}

@custom-variant data-open {
  &:where([data-state="open"]),
  &:where([data-open]:not([data-open="false"])) {
    @slot;
  }
}

@utility no-scrollbar {
  -ms-overflow-style: none;
  scrollbar-width: none;

  &::-webkit-scrollbar {
    display: none;
  }
}
```

**モノレポ**

モノレポの場合、`components.json` とグローバル CSS ファイルが含まれるワークスペースからコマンドを実行します：

```bash
npx shadcn@latest eject -c packages/ui
```

**オプション**

```bash
Usage: shadcn eject [options]

inline shadcn/tailwind.css and remove the shadcn dependency

Options:
  -c, --cwd <cwd>  the working directory. defaults to the current directory.
  -y, --yes        skip confirmation prompt. (default: false)
  -s, --silent     mute output. (default: false)
  -h, --help       display help for command
```
