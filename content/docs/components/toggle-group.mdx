---
title: Toggle Group
description: オン/オフを切り替えられる2ステートボタンのセット。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/toggle-group
  api: https://base-ui.com/react/components/toggle-group#api-reference
---

<ComponentPreview styleName="base-nova" name="toggle-group-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add toggle-group
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下の依存パッケージをインストールします:</Step>

```bash
npm install @base-ui/react
```

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="toggle-group"
  title="components/ui/toggle-group.tsx"
  styleName="base-nova"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { ToggleGroup, ToggleGroupItem } from "@/components/ui/toggle-group"
```

```tsx
<ToggleGroup type="single">
  <ToggleGroupItem value="a">A</ToggleGroupItem>
  <ToggleGroupItem value="b">B</ToggleGroupItem>
  <ToggleGroupItem value="c">C</ToggleGroupItem>
</ToggleGroup>
```

## 構成

`ToggleGroup` の構成:

```text
ToggleGroup
├── ToggleGroupItem
└── ToggleGroupItem
```

## 使用例

### Outline

`variant="outline"` でアウトラインスタイルにします。

<ComponentPreview styleName="base-nova" name="toggle-group-outline" />

### Size

`size` でトグルグループのサイズを変更できます。

<ComponentPreview styleName="base-nova" name="toggle-group-sizes" />

### Spacing

`spacing` でトグルグループ項目間に間距を追加します。

<ComponentPreview styleName="base-nova" name="toggle-group-spacing" />

### Vertical

`orientation="vertical"` で縦向きのトグルグループにします。

<ComponentPreview styleName="base-nova" name="toggle-group-vertical" />

### Disabled

<ComponentPreview styleName="base-nova" name="toggle-group-disabled" />

### Custom

トグルグループのカスタム例。

<ComponentPreview
  styleName="base-nova"
  name="toggle-group-font-weight-selector"
  previewClassName="*:data-[slot=field]:max-w-xs"
/>

## RTL

<ComponentPreview
  styleName="base-nova"
  name="toggle-group-rtl"
  direction="rtl"
/>

## API リファレンス

詳細は [Base UI Toggle Group](https://base-ui.com/react/components/toggle-group#api-reference) ドキュメントを参照してください。

## 変更履歴

### 2026-05-17 デフォルト間距

デフォルトの `spacing` を `0` から `2` に変更し、トグルグループがデフォルトで順に間距を置いてレンダリングするようにしました。連結した項目には `spacing={0}` を使用してください。
