---
title: Aspect Ratio
description: 指定したアスペクト比でコンテンツを表示します。
base: base
component: true
---

<ComponentPreview name="aspect-ratio-demo" styleName="base" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.tyap.me/r/styles/base/aspect-ratio.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下の依存パッケージをインストールします:</Step>

```bash
npm install @base-ui/react
```

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="aspect-ratio"
  title="components/ui/aspect-ratio.tsx"
  styleName="base"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import { AspectRatio } from "@/components/ui/aspect-ratio"
```

```tsx showLineNumbers
<AspectRatio ratio={16 / 9}>
  <Image src="..." alt="Image" className="rounded-md object-cover" />
</AspectRatio>
```

## 使用例

### Square

`ratio={1 / 1}` を使った正方形のアスペクト比コンポーネント。画像を正方形で表示するのに便利です。

<ComponentPreview name="aspect-ratio-square" styleName="base" />

### Portrait

`ratio={9 / 16}` を使った縦長のアスペクト比コンポーネント。画像を縦長で表示するのに便利です。

<ComponentPreview
  styleName="base"
  name="aspect-ratio-portrait"
  previewClassName="h-96"
/>

## RTL

<ComponentPreview
  styleName="base"
  name="aspect-ratio-rtl"
  direction="rtl"
  previewClassName="h-96"
/>

## API リファレンス

### AspectRatio

`AspectRatio` コンポーネントは指定したアスペクト比でコンテンツを表示します。

| Prop        | Type     | Default | Required |
| ----------- | -------- | ------- | -------- |
| `ratio`     | `number` | -       | Yes      |
| `className` | `string` | -       | No       |

詳細は [Base UI ドキュメント](https://base-ui.com/react/components/aspect-ratio#api-reference) を参照してください。
