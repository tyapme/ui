---
title: Spinner
description: ローディング状態を表示するインジケーター。
base: base
component: true
---

<ComponentPreview styleName="base-nova" name="spinner-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add spinner
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="spinner"
  title="components/ui/spinner.tsx"
  styleName="base-nova"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { Spinner } from "@/components/ui/spinner"
```

```tsx
<Spinner />
```

## カスタマイズ

`Spinner` コンポーネントを編集することで、デフォルトのスピナーアイコンを別のアイコンに変更できます。

<ComponentPreview styleName="base-nova" name="spinner-custom" />

```tsx showLineNumbers title="components/ui/spinner.tsx"
import { LoaderIcon } from "lucide-react"

import { cn } from "@/lib/utils"

function Spinner({ className, ...props }: React.ComponentProps<"svg">) {
  return (
    <LoaderIcon
      role="status"
      aria-label="Loading"
      className={cn("size-4 animate-spin", className)}
      {...props}
    />
  )
}

export { Spinner }
```

## 使用例

### Size

`size-*` ユーティリティクラスでスピナーのサイズを変更できます。

<ComponentPreview styleName="base-nova" name="spinner-size" />

### Button

ボタンにスピナーを追加してローディング状態を示します。開始位置には `data-icon="inline-start"` を付けてラベルの前に、終了位置には `data-icon="inline-end"` を付けてラベルの後に `<Spinner />` を配置します。

<ComponentPreview styleName="base-nova" name="spinner-button" />

### Badge

バッジにスピナーを追加してローディング状態を示します。開始位置には `data-icon="inline-start"` を付けてラベルの前に、終了位置には `data-icon="inline-end"` を付けてラベルの後に `<Spinner />` を配置します。

<ComponentPreview styleName="base-nova" name="spinner-badge" />

### Input Group

<ComponentPreview styleName="base-nova" name="spinner-input-group" />

### Empty

<ComponentPreview styleName="base-nova" name="spinner-empty" />

## RTL

<ComponentPreview styleName="base-nova" name="spinner-rtl" direction="rtl" />
