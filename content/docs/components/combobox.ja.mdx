---
title: Combobox
description: 候補リスト付きのオートコンプリート入力。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/combobox
  api: https://base-ui.com/react/components/combobox#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="combobox-demo"
  description="A combobox with a list of frameworks."
/>

## インストール

<ComponentInstall name="combobox" styleName="base-nova" />

## 使い方

```tsx showLineNumbers
import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from "@/components/ui/combobox"
```

```tsx showLineNumbers
const frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

export function ExampleCombobox() {
  return (
    <Combobox items={frameworks}>
      <ComboboxInput placeholder="Select a framework" />
      <ComboboxContent>
        <ComboboxEmpty>No items found.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item} value={item}>
              {item}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 構成

### シンプル

1行の入力とフラットなリスト（[基本](#基本) を参照）。

```text
Combobox
├── ComboboxInput
└── ComboboxContent
    ├── ComboboxEmpty
    └── ComboboxList
        ├── ComboboxItem
        └── ComboboxItem
```

### チップ付き

`multiple`、チップ、チップ入力を使用した複数選択（[複数選択](#複数選択) を参照）。

```text
Combobox
├── ComboboxChips
│   ├── ComboboxValue
│   │   └── ComboboxChip
│   └── ComboboxChipsInput
└── ComboboxContent
    ├── ComboboxEmpty
    └── ComboboxList
        ├── ComboboxItem
        └── ComboboxItem
```

### グループとコレクション付き

各 `ComboboxGroup` 内に `ComboboxCollection` を使ったネストされたアイテム、グループ間にセパレーター付き（[グループ](#グループ) を参照）。

```text
Combobox
├── ComboboxInput
└── ComboboxContent
    ├── ComboboxEmpty
    └── ComboboxList
        ├── ComboboxGroup
        │   ├── ComboboxLabel
        │   └── ComboboxCollection
        │       ├── ComboboxItem
        │       └── ComboboxItem
        ├── ComboboxSeparator
        └── ComboboxGroup
            ├── ComboboxLabel
            └── ComboboxCollection
                ├── ComboboxItem
                └── ComboboxItem
```

## カスタムアイテム

アイテムがオブジェクトの場合は `itemToStringValue` を使用します。

```tsx showLineNumbers
import * as React from "react"

import {
  Combobox,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
} from "@/components/ui/combobox"

type Framework = {
  label: string
  value: string
}

const frameworks: Framework[] = [
  { label: "Next.js", value: "next" },
  { label: "SvelteKit", value: "sveltekit" },
  { label: "Nuxt", value: "nuxt" },
]

export function ExampleComboboxCustomItems() {
  return (
    <Combobox
      items={frameworks}
      itemToStringValue={(framework) => framework.label}
    >
      <ComboboxInput placeholder="Select a framework" />
      <ComboboxContent>
        <ComboboxEmpty>No items found.</ComboboxEmpty>
        <ComboboxList>
          {(framework) => (
            <ComboboxItem key={framework.value} value={framework}>
              {framework.label}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 複数選択

チップを使った複数選択には `multiple` を使用します。

```tsx showLineNumbers
import * as React from "react"

import {
  Combobox,
  ComboboxChip,
  ComboboxChips,
  ComboboxChipsInput,
  ComboboxContent,
  ComboboxEmpty,
  ComboboxInput,
  ComboboxItem,
  ComboboxList,
  ComboboxValue,
} from "@/components/ui/combobox"

const frameworks = ["Next.js", "SvelteKit", "Nuxt.js", "Remix", "Astro"]

export function ExampleComboboxMultiple() {
  const [value, setValue] = React.useState<string[]>([])

  return (
    <Combobox
      items={frameworks}
      multiple
      value={value}
      onValueChange={setValue}
    >
      <ComboboxChips>
        <ComboboxValue>
          {value.map((item) => (
            <ComboboxChip key={item}>{item}</ComboboxChip>
          ))}
        </ComboboxValue>
        <ComboboxChipsInput placeholder="Add framework" />
      </ComboboxChips>
      <ComboboxContent>
        <ComboboxEmpty>No items found.</ComboboxEmpty>
        <ComboboxList>
          {(item) => (
            <ComboboxItem key={item} value={item}>
              {item}
            </ComboboxItem>
          )}
        </ComboboxList>
      </ComboboxContent>
    </Combobox>
  )
}
```

## 例

### 基本

フレームワークのリストを持つシンプルなコンボボックス。

<ComponentPreview styleName="base-nova" name="combobox-basic" />

### 複数選択

`multiple` と `ComboboxChips` を使った複数選択コンボボックス。

<ComponentPreview styleName="base-nova" name="combobox-multiple" />

### クリアボタン

`showClear` プロパティを使用してクリアボタンを表示します。

<ComponentPreview styleName="base-nova" name="combobox-clear" />

### グループ

`ComboboxGroup` と `ComboboxSeparator` を使ってアイテムをグループ化します。

<ComponentPreview styleName="base-nova" name="combobox-groups" />

### カスタムアイテム

`ComboboxItem` 内にカスタムコンポーネントをレンダリングできます。

<ComponentPreview styleName="base-nova" name="combobox-custom" />

### 無効

`aria-invalid` プロパティを使ってコンボボックスを無効状態にします。

<ComponentPreview styleName="base-nova" name="combobox-invalid" />

### 無効化

`disabled` プロパティを使ってコンボボックスを無効化します。

<ComponentPreview styleName="base-nova" name="combobox-disabled" />

### オートハイライト

`autoHighlight` プロパティを使ってフィルター時に最初のアイテムを自動的にハイライトします。

<ComponentPreview styleName="base-nova" name="combobox-auto-highlight" />

### ポップアップ

`render` プロパティを使ってボタンや他のコンポーネントからコンボボックスをトリガーできます。`ComboboxInput` を `ComboboxContent` 内に移動してください。

<ComponentPreview styleName="base-nova" name="combobox-popup" />

### 入力グループ

`ComboboxInput` 内で `InputGroupAddon` コンポーネントを使ってコンボボックスにアドオンを追加できます。

<ComponentPreview styleName="base-nova" name="combobox-input-group" />

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 設定ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="combobox-rtl"
  direction="rtl"
  align="start"
/>

## API リファレンス

詳細については [Base UI](https://base-ui.com/react/components/combobox#api-reference) のドキュメントを参照してください。
