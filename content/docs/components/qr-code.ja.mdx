---
title: QR Code
description: ファインダーパターンを角丸、データモジュールをドットで描画する、スタイリッシュな QR コードコンポーネント。
component: true
base: base
---

文字列や URL をエンコードして QR コードを生成します。3 つのファインダーパターン（角の四角）は角丸のリングで、データモジュールは円（ドット）で描画されるため、通常の四角い QR よりも柔らかい印象になります。色は CSS 変数をそのまま渡せるので、テーマに追従させることもできます。

<ComponentPreview styleName="base-nova" name="qr-code-demo" align="start" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>

<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.tyap.me/r/styles/base/qr-code.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下の依存パッケージをインストールします:</Step>

```bash
npm install qrcode
```

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="qr-code"
  title="components/ui/qr-code.tsx"
  styleName="base-nova"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { QRCode } from "@/components/ui/qr-code"
```

```tsx
<QRCode value="https://example.com" />
<QRCode value="https://example.com" size={140} />
```

## サンプル

### カラー

`fgColor` / `bgColor` には任意の CSS カラーや CSS 変数を渡せます。デフォルトは `var(--foreground)` / `var(--background)` で、テーマに追従します。

<ComponentPreview styleName="base-nova" name="qr-code-colors" align="start" />

## Props

`<svg>` の属性に加えて、以下を受け取ります。

| Prop                   | Type                          | Default              | Description                                              |
| :--------------------- | :---------------------------- | :------------------- | :------------------------------------------------------ |
| `value`                | `string`                      | -                    | `必須`。エンコードする文字列または URL。                |
| `size`                 | `number`                      | `268`                | QR コードのサイズ（ピクセル）。                         |
| `fgColor`              | `string`                      | `var(--foreground)`  | 前景（暗い部分）の色。CSS 変数も指定できます。          |
| `bgColor`              | `string`                      | `var(--background)`  | 背景（明るい部分）の色。CSS 変数も指定できます。        |
| `errorCorrectionLevel` | `"L" \| "M" \| "Q" \| "H"`    | `"M"`                | 誤り訂正レベル。L: 7%、M: 15%、Q: 25%、H: 30%。         |
| `className`            | `string`                      | -                    | 追加の CSS クラス。                                      |
