---
title: Table
description: レスポンシブ対応のテーブルコンポーネント。
base: base
component: true
---

<ComponentPreview
  styleName="base"
  name="table-demo"
  previewClassName="h-[30rem]"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.tyap.me/r/styles/base/table.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="table"
  title="components/ui/table.tsx"
  styleName="base"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Table,
  TableBody,
  TableCaption,
  TableCell,
  TableHead,
  TableHeader,
  TableRow,
} from "@/components/ui/table"
```

```tsx showLineNumbers
<Table>
  <TableCaption>A list of your recent invoices.</TableCaption>
  <TableHeader>
    <TableRow>
      <TableHead className="w-[100px]">Invoice</TableHead>
      <TableHead>Status</TableHead>
      <TableHead>Method</TableHead>
      <TableHead className="text-right">Amount</TableHead>
    </TableRow>
  </TableHeader>
  <TableBody>
    <TableRow>
      <TableCell className="font-medium">INV001</TableCell>
      <TableCell>Paid</TableCell>
      <TableCell>Credit Card</TableCell>
      <TableCell className="text-right">$250.00</TableCell>
    </TableRow>
  </TableBody>
</Table>
```

## 構成

`Table` の構成:

```text
Table
├── TableCaption
├── TableHeader
│   └── TableRow
│       ├── TableHead
│       ├── TableHead
│       ├── TableHead
│       └── TableHead
├── TableBody
│   ├── TableRow
│   │   ├── TableCell
│   │   ├── TableCell
│   │   ├── TableCell
│   │   └── TableCell
│   └── TableRow
│       ├── TableCell
│       ├── TableCell
│       ├── TableCell
│       └── TableCell
└── TableFooter
```

## 使用例

### Footer

`<TableFooter />` でテーブルにフッターを追加します。

<ComponentPreview styleName="base" name="table-footer" />

### Actions

`<DropdownMenu />` を用いて各行にアクションを表示するテーブル。

<ComponentPreview styleName="base" name="table-actions" />

## Data Table

`<Table />` を使ってより複雑なデータテーブルを構築できます。[@tanstack/react-table](https://tanstack.com/table/v8) と組み合わせることで、ソート・フィルター・ページネーション付きのテーブルを作成できます。

詳細は [Data Table](/docs/components/data-table) ドキュメントを参照してください。

## RTL

<ComponentPreview
  styleName="base"
  name="table-rtl"
  direction="rtl"
  previewClassName="h-auto"
/>
