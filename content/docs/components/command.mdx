---
title: Command
description: 検索とクイックアクションのためのコマンドメニュー。
base: base
component: true
links:
  doc: https://github.com/dip/cmdk
---

<ComponentPreview
  styleName="base"
  name="command-demo"
  align="start"
  previewClassName="h-[24.5rem]"
/>

## About

`<Command />` コンポーネントは [Dip](https://www.dip.org/) による [`cmdk`](https://github.com/dip/cmdk) コンポーネントを使用しています。

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.tyap.me/r/styles/base/command.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下の依存パッケージをインストールします:</Step>

```bash
npm install cmdk
```

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="command"
  title="components/ui/command.tsx"
  styleName="base"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Command,
  CommandDialog,
  CommandEmpty,
  CommandGroup,
  CommandInput,
  CommandItem,
  CommandList,
  CommandSeparator,
  CommandShortcut,
} from "@/components/ui/command"
```

```tsx showLineNumbers
<Command className="max-w-sm rounded-lg border">
  <CommandInput placeholder="Type a command or search..." />
  <CommandList>
    <CommandEmpty>No results found.</CommandEmpty>
    <CommandGroup heading="Suggestions">
      <CommandItem>Calendar</CommandItem>
      <CommandItem>Search Emoji</CommandItem>
      <CommandItem>Calculator</CommandItem>
    </CommandGroup>
    <CommandSeparator />
    <CommandGroup heading="Settings">
      <CommandItem>Profile</CommandItem>
      <CommandItem>Billing</CommandItem>
      <CommandItem>Settings</CommandItem>
    </CommandGroup>
  </CommandList>
</Command>
```

## 構成

`Command` の構成:

```text
Command
├── CommandInput
└── CommandList
    ├── CommandEmpty
    ├── CommandGroup
    │   ├── CommandItem
    │   └── CommandItem
    ├── CommandSeparator
    └── CommandGroup
        ├── CommandItem
        └── CommandItem
```

## 使用例

### Basic

ダイアログ内のシンプルなコマンドメニュー。

<ComponentPreview styleName="base" name="command-basic" />

### Shortcuts

<ComponentPreview styleName="base" name="command-shortcuts" />

### Groups

グループ・アイコン・セパレーター付きのコマンドメニュー。

<ComponentPreview styleName="base" name="command-groups" />

### Scrollable

複数アイテムがスクロール可能なコマンドメニュー。

<ComponentPreview styleName="base" name="command-scrollable" />

## RTL

<ComponentPreview
  styleName="base"
  name="command-rtl"
  direction="rtl"
  align="start"
  previewClassName="h-[24.5rem]"
/>

## API リファレンス

詳細は [cmdk](https://github.com/dip/cmdk) のドキュメントを参照してください。
