---
title: Collapsible
description: パネルを展開・折りたたみできるインタラクティブなコンポーネント。
base: base
component: true
featured: true
links:
  doc: https://base-ui.com/react/components/collapsible
  api: https://base-ui.com/react/components/collapsible#api-reference
---

<ComponentPreview styleName="base-nova" name="collapsible-demo" align="start" />

## インストール

<ComponentInstall name="collapsible" styleName="base-nova" />

## 使い方

```tsx showLineNumbers
import {
  Collapsible,
  CollapsibleContent,
  CollapsibleTrigger,
} from "@/components/ui/collapsible"
```

```tsx showLineNumbers
<Collapsible>
  <CollapsibleTrigger>このプロジェクトで使用できますか？</CollapsibleTrigger>
  <CollapsibleContent>
    はい。個人・商用プロジェクトで無料で使用できます。帰属表示は不要です。
  </CollapsibleContent>
</Collapsible>
```

## 構成

`Collapsible` を構築するには以下の構成を使用します:

```text
Collapsible
├── CollapsibleTrigger
└── CollapsibleContent
```

## 制御された状態

`open` と `onOpenChange` プロパティを使用して状態を制御します。

```tsx showLineNumbers
import * as React from "react"

export function Example() {
  const [open, setOpen] = React.useState(false)

  return (
    <Collapsible open={open} onOpenChange={setOpen}>
      <CollapsibleTrigger>トグル</CollapsibleTrigger>
      <CollapsibleContent>コンテンツ</CollapsibleContent>
    </Collapsible>
  )
}
```

## 例

### 基本

<ComponentPreview
  styleName="base-nova"
  name="collapsible-basic"
  align="start"
/>

### 設定パネル

トリガーボタンを使用して追加の設定を表示します。

<ComponentPreview styleName="base-nova" name="collapsible-settings" />

### ファイルツリー

ネストされた Collapsible を使ってファイルツリーを構築します。

<ComponentPreview
  styleName="base-nova"
  name="collapsible-file-tree"
  previewClassName="h-[36rem]"
/>

## RTL

shadcn/ui で RTL サポートを有効にするには、[RTL 設定ガイド](/docs/rtl) を参照してください。

<ComponentPreview
  styleName="base-nova"
  name="collapsible-rtl"
  direction="rtl"
  align="start"
/>

## API リファレンス

詳細については [Base UI](https://base-ui.com/react/components/collapsible#api-reference) のドキュメントを参照してください。
