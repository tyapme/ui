---
title: Sheet
description: Dialog コンポーネントを拡張し、メインコンテンツを補完するコンテンツを表示します。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/dialog
  api: https://base-ui.com/react/components/dialog#api-reference
---

<ComponentPreview styleName="base-nova" name="sheet-demo" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add sheet
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下の依存パッケージをインストールします:</Step>

```bash
npm install @base-ui/react
```

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="sheet"
  title="components/ui/sheet.tsx"
  styleName="base-nova"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Sheet,
  SheetClose,
  SheetContent,
  SheetDescription,
  SheetFooter,
  SheetHeader,
  SheetTitle,
  SheetTrigger,
} from "@/components/ui/sheet"
```

```tsx showLineNumbers
<Sheet>
  <SheetTrigger>Open</SheetTrigger>
  <SheetContent>
    <SheetHeader>
      <SheetTitle>Are you absolutely sure?</SheetTitle>
      <SheetDescription>This action cannot be undone.</SheetDescription>
    </SheetHeader>
  </SheetContent>
</Sheet>
```

## 構成

`Sheet` の構成:

```text
Sheet
├── SheetTrigger
└── SheetContent
    ├── SheetHeader
    │   ├── SheetTitle
    │   └── SheetDescription
    └── SheetFooter
```

## 使用例

### 方向

`SheetContent` の `side` でシートが表示される画面の端を設定します。`top`、`right`、`bottom`、`left` が利用可能です。

<ComponentPreview styleName="base-nova" name="sheet-side" />

### 閉じるボタンなし

`SheetContent` の `showCloseButton={false}` で閉じるボタンを非表示にします。

<ComponentPreview styleName="base-nova" name="sheet-no-close-button" />

## RTL

<ComponentPreview styleName="base-nova" name="sheet-rtl" direction="rtl" />

## API リファレンス

詳細は [Base UI Dialog](https://base-ui.com/react/components/dialog#api-reference) ドキュメントを参照してください。
