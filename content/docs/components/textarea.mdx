---
title: Textarea
description: フォーム用テキストエリアまたはテキストエリアのようなコンポーネントを表示します。
base: base
component: true
---

<ComponentPreview
  styleName="base"
  name="textarea-demo"
  previewClassName="*:max-w-xs"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.tyap.me/r/styles/base/textarea.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="textarea"
  title="components/ui/textarea.tsx"
  styleName="base"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx
import { Textarea } from "@/components/ui/textarea"
```

```tsx
<Textarea />
```

## 使用例

### Field

`Field`、`FieldLabel`、`FieldDescription` でラベルと説明付きテキストエリアを作成します。

<ComponentPreview
  styleName="base"
  name="textarea-field"
  previewClassName="*:max-w-xs"
/>

### Disabled

`disabled` でテキストエリアを無効化します。無効状態のスタイリングには `Field` に `data-disabled` を付けます。

<ComponentPreview
  styleName="base"
  name="textarea-disabled"
  previewClassName="*:max-w-xs"
/>

### Invalid

`aria-invalid` でテキストエリアを未入力状態にします。未入力状態のスタイリングには `Field` に `data-invalid` を付けます。

<ComponentPreview
  styleName="base"
  name="textarea-invalid"
  previewClassName="*:max-w-xs"
/>

### Button

`Button` と組み合わせて送信ボタン付きテキストエリアを作成します。

<ComponentPreview
  styleName="base"
  name="textarea-button"
  previewClassName="*:max-w-xs"
/>

## RTL

<ComponentPreview styleName="base" name="textarea-rtl" direction="rtl" />
