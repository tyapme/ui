---
title: Context Menu
description: 右クリックで表示されるアクションメニュー。
base: base
component: true
links:
  doc: https://base-ui.com/react/components/context-menu
  api: https://base-ui.com/react/components/context-menu#api-reference
---

<ComponentPreview
  styleName="base-nova"
  name="context-menu-demo"
  description="サブメニュー付きのコンテキストメニュー。"
/>

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">コマンド</TabsTrigger>
  <TabsTrigger value="manual">手動</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add context-menu
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下の依存パッケージをインストールします:</Step>

```bash
npm install @base-ui/react
```

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="context-menu"
  title="components/ui/context-menu.tsx"
  styleName="base-nova"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  ContextMenu,
  ContextMenuContent,
  ContextMenuItem,
  ContextMenuTrigger,
} from "@/components/ui/context-menu"
```

```tsx showLineNumbers
<ContextMenu>
  <ContextMenuTrigger>Right click here</ContextMenuTrigger>
  <ContextMenuContent>
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## 構成

`ContextMenu` の構成:

```text
ContextMenu
├── ContextMenuTrigger
└── ContextMenuContent
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuItem
    │   └── ContextMenuItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   ├── ContextMenuCheckboxItem
    │   └── ContextMenuCheckboxItem
    ├── ContextMenuSeparator
    ├── ContextMenuGroup
    │   ├── ContextMenuLabel
    │   └── ContextMenuRadioGroup
    │       ├── ContextMenuRadioItem
    │       └── ContextMenuRadioItem
    └── ContextMenuSub
        ├── ContextMenuSubTrigger
        └── ContextMenuSubContent
            └── ContextMenuGroup
                ├── ContextMenuItem
                └── ContextMenuItem
```

## 使用例

### Basic

アクションをイクツか持つシンプルなコンテキストメニュー。

<ComponentPreview styleName="base-nova" name="context-menu-basic" />

### Submenu

`ContextMenuSub` でサブアクションをネストします。

<ComponentPreview styleName="base-nova" name="context-menu-submenu" />

### Shortcuts

`ContextMenuShortcut` でキーボードヒントを表示します。

<ComponentPreview styleName="base-nova" name="context-menu-shortcuts" />

### Groups

関連するアクションをまとめ、ディバイダーで区切ります。

<ComponentPreview styleName="base-nova" name="context-menu-groups" />

### Icons

アイコンとラベルを組み合わせて視覚的にわかりやすくします。

<ComponentPreview styleName="base-nova" name="context-menu-icons" />

### Checkboxes

トグルには `ContextMenuCheckboxItem` を使います。

<ComponentPreview styleName="base-nova" name="context-menu-checkboxes" />

### Radio

排他選択には `ContextMenuRadioItem` を使います。

<ComponentPreview styleName="base-nova" name="context-menu-radio" />

### Destructive

`variant="destructive"` でメニューアイテムを破壊的なスタイルにします。

<ComponentPreview styleName="base-nova" name="context-menu-destructive" />

### Sides

side と align props でサブメニューの配置を制御します。

<ComponentPreview styleName="base-nova" name="context-menu-sides" />

## RTL

<ComponentPreview
  styleName="base-nova"
  name="context-menu-rtl"
  direction="rtl"
/>

`side="inline-end"` でトリガーの論理的右側にメニューを配置します。

```tsx showLineNumbers
<ContextMenu>
  <ContextMenuTrigger>Right click here</ContextMenuTrigger>
  <ContextMenuContent side="inline-end">
    <ContextMenuItem>Profile</ContextMenuItem>
    <ContextMenuItem>Billing</ContextMenuItem>
    <ContextMenuItem>Team</ContextMenuItem>
    <ContextMenuItem>Subscription</ContextMenuItem>
  </ContextMenuContent>
</ContextMenu>
```

## API リファレンス

詳細は [Base UI](https://base-ui.com/react/components/context-menu#api-reference) のドキュメントを参照してください。
