---
title: Collapsible
description: パネルを展開・折りたたみできるインタラクティブなコンポーネント。
base: base
component: true
featured: true
links:
  doc: https://base-ui.com/react/components/collapsible
  api: https://base-ui.com/react/components/collapsible#api-reference
---

<ComponentPreview styleName="base" name="collapsible-demo" align="start" />

## インストール

<CodeTabs>

<TabsList>
  <TabsTrigger value="cli">Command</TabsTrigger>
  <TabsTrigger value="manual">Manual</TabsTrigger>
</TabsList>
<TabsContent value="cli">

```bash
npx shadcn@latest add https://ui.tyap.me/r/styles/base/collapsible.json
```

</TabsContent>

<TabsContent value="manual">

<Steps className="mb-0 pt-2">

<Step>以下の依存パッケージをインストールします:</Step>

```bash
npm install @base-ui/react
```

<Step>以下のコードをプロジェクトにコピーします。</Step>

<ComponentSource
  name="collapsible"
  title="components/ui/collapsible.tsx"
  styleName="base"
/>

<Step>import パスをプロジェクト構成に合わせて更新します。</Step>

</Steps>

</TabsContent>

</CodeTabs>

## 使い方

```tsx showLineNumbers
import {
  Collapsible,
  CollapsibleContent,
  CollapsibleTrigger,
} from "@/components/ui/collapsible"
```

```tsx showLineNumbers
<Collapsible>
  <CollapsibleTrigger>Can I use this in my project?</CollapsibleTrigger>
  <CollapsibleContent>
    Yes. Free to use for personal and commercial projects. No attribution
    required.
  </CollapsibleContent>
</Collapsible>
```

## 構成

`Collapsible` の構成:

```text
Collapsible
├── CollapsibleTrigger
└── CollapsibleContent
```

## 制御状態

`open` と `onOpenChange` props で開閉状態を制御します。

```tsx showLineNumbers
import * as React from "react"

export function Example() {
  const [open, setOpen] = React.useState(false)

  return (
    <Collapsible open={open} onOpenChange={setOpen}>
      <CollapsibleTrigger>Toggle</CollapsibleTrigger>
      <CollapsibleContent>Content</CollapsibleContent>
    </Collapsible>
  )
}
```

## 使用例

### Basic

<ComponentPreview
  styleName="base"
  name="collapsible-basic"
  align="start"
/>

### Settings Panel

トリガーボタンで追加の設定を表示します。

<ComponentPreview styleName="base" name="collapsible-settings" />

### File Tree

ネストした Collapsible でファイルツリーを構築します。

<ComponentPreview
  styleName="base"
  name="collapsible-file-tree"
  previewClassName="h-[36rem]"
/>

## RTL

<ComponentPreview
  styleName="base"
  name="collapsible-rtl"
  direction="rtl"
  align="start"
/>

## API リファレンス

詳細は [Base UI](https://base-ui.com/react/components/collapsible#api-reference) のドキュメントを参照してください。
