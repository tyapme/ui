---
title: Next.js
description: useActionState と Server Actions を使った React フォーム構築。
---

import { InfoIcon } from "lucide-react"

このガイドでは、`useActionState` と Server Actions を使った Next.js フォーム構築を解説します。フォーム作成、検証、ペンディング状態、アクセシビリティなどをカバーします。

## デモ

シンプルなテキスト入力とテキストエリアを持つ以下のフォームを作成します。送信時には、Server Action でフォームデータを検証してフォーム状態を更新します。

<ComponentPreview
  name="form-next-demo"
  className="[&_.preview]:h-[700px] [&_pre]:h-[700px]!"
/>

<Callout icon={<InfoIcon />}>
  **注意:** このページの例は、Server Actions でのスキーマ検証とフォームエラーの動作を示すため、意図的にブラウザ検証を無効にしています。
</Callout>

## アプローチ

このフォームは、Next.js と React のビルトイン機能を活用してフォーム処理を実現しています。**マークアップとスタイリングを完全にコントロールできる** `<Field />` コンポーネントを使ってフォームを構築します。

- ナビゲーションとプログレッシブエンハンスメントのために Next.js `<Form />` コンポーネントを使用します。
- アクセシブルなフォーム構築のために `<Field />` コンポーネントを使用します。
- フォーム状態とエラー管理に `useActionState` を使用します。
- pending プロップでローディング状態を処理します。
- フォーム送信の処理に Server Actions を使用します。
- Zod によるサーバーサイド検証。

## アナトミー

`<Field />` コンポーネントを使ったフォームの基本例。

```tsx showLineNumbers
<Form action={formAction}>
  <FieldGroup>
    <Field data-invalid={!!formState.errors?.title?.length}>
      <FieldLabel htmlFor="title">Bug Title</FieldLabel>
      <Input
        id="title"
        name="title"
        defaultValue={formState.values.title}
        disabled={pending}
        aria-invalid={!!formState.errors?.title?.length}
        placeholder="Login button not working on mobile"
        autoComplete="off"
      />
      <FieldDescription>
        Provide a concise title for your bug report.
      </FieldDescription>
      {formState.errors?.title && (
        <FieldError>{formState.errors.title[0]}</FieldError>
      )}
    </Field>
  </FieldGroup>
  <Button type="submit">Submit</Button>
</Form>
```

## 使い方

### フォームスキーマの作成

`schema.ts` ファイルに Zod スキーマでフォームの形式を定義します。

<Callout icon={<InfoIcon />}>
  **注意:** この例では `zod v3` を使用していますが、他のスキーマ検証ライブラリに置き換えて使用できます。スキーマライブラリが Standard Schema 仕様に準拠していることを確認してください。
</Callout>

```tsx showLineNumbers title="schema.ts"
import { z } from "zod"

export const formSchema = z.object({
  title: z
    .string()
    .min(5, "Bug title must be at least 5 characters.")
    .max(32, "Bug title must be at most 32 characters."),
  description: z
    .string()
    .min(20, "Description must be at least 20 characters.")
    .max(100, "Description must be at most 100 characters."),
})
```

### フォーム状態型の定義

値、エラー、成功ステータスを含むフォーム状態の型を作成します。クライアントとサーバー両方でフォーム状態を型付けするために使用します。

```tsx showLineNumbers title="schema.ts"
import { z } from "zod"

export type FormState = {
  values?: z.infer<typeof formSchema>
  errors: null | Partial<Record<keyof z.infer<typeof formSchema>, string[]>>
  success: boolean
}
```

**重要:** スキーマと `FormState` 型はクライアントとサーバー両方のコンポーネントからインポートできるよう、別ファイルに定義してください。

### Server Action の作成

Server Action はサーバーで実行され、クライアントから呼び出すことができる関数です。フォームデータの検証とフォーム状態の更新に使用します。

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-demo-action.ts"
  title="actions.ts"
/>

**注意:** エラー時に `values` を返すことで、ユーザーが入力した値をフォーム状態に保持します。成功時は空の values を返してフォームをリセットします。

### フォームの構築

`<Field />` コンポーネントでフォームを作成できます。`useActionState` フックでフォーム状態、Server Action、pending 状態を管理します。

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-demo.tsx"
  title="form.tsx"
/>

### 完成

以上です。クライアントおよびサーバーサイド検証付きの完全なアクセシブルなフォームができました。

フォームを送信すると、サーバーで `formAction` 関数が呼び出されます。Server Action がフォームデータを検証し、フォーム状態を更新します。

フォームデータが無効な場合はエラーがクライアントに返されます。有効な場合は成功ステータスが返され、フォーム状態が更新されます。

## ペンディング状態

`useActionState` の `pending` プロップでローディングインジケータを表示し、フォーム入力を無効化します。

```tsx showLineNumbers {11,26-34}
"use client"

import * as React from "react"
import Form from "next/form"

import { Spinner } from "@/components/ui/spinner"

import { bugReportFormAction } from "./actions"

export function BugReportForm() {
  const [formState, formAction, pending] = React.useActionState(
    bugReportFormAction,
    {
      errors: null,
      success: false,
    }
  )

  return (
    <Form action={formAction}>
      <FieldGroup>
        <Field data-disabled={pending}>
          <FieldLabel htmlFor="name">Name</FieldLabel>
          <Input id="name" name="name" disabled={pending} />
        </Field>
        <Field>
          <Button type="submit" disabled={pending}>
            {pending && <Spinner />} Submit
          </Button>
        </Field>
      </FieldGroup>
    </Form>
  )
}
```

## 無効状態

### 送信ボタン

送信ボタンを無効にするには、`disabled` プロップに `pending` を使用します。

```tsx showLineNumbers
<Button type="submit" disabled={pending}>
  {pending && <Spinner />} Submit
</Button>
```

### フィールド

`<Field />` コンポーネントに無効状態とスタイリングを適用するには、`data-disabled` を使用します。

```tsx showLineNumbers
<Field data-disabled={pending}>
  <FieldLabel htmlFor="name">Name</FieldLabel>
  <Input id="name" name="name" disabled={pending} />
</Field>
```

## 検証

### サーバーサイド検証

Server Action 内で `safeParse()` を使ってフォームデータを検証します。

```tsx showLineNumbers title="actions.ts" {12-20}
"use server"

export async function bugReportFormAction(
  _prevState: FormState,
  formData: FormData
) {
  const values = {
    title: formData.get("title") as string,
    description: formData.get("description") as string,
  }

  const result = formSchema.safeParse(values)

  if (!result.success) {
    return {
      values,
      success: false,
      errors: result.error.flatten().fieldErrors,
    }
  }

  return {
    errors: null,
    success: true,
  }
}
```

### ビジネスロジックの検証

Server Action 内に独自のカスタム検証ロジックを追加できます。

検証エラー時には必ず values を返すようにしてください。ユーザーの入力内容をフォーム状態に保持するためです。

```tsx showLineNumbers title="actions.ts" {22-35}
"use server"

export async function bugReportFormAction(
  _prevState: FormState,
  formData: FormData
) {
  const values = {
    title: formData.get("title") as string,
    description: formData.get("description") as string,
  }

  const result = formSchema.safeParse(values)

  if (!result.success) {
    return {
      values,
      success: false,
      errors: result.error.flatten().fieldErrors,
    }
  }

  // Check if email already exists in database.
  const existingUser = await db.user.findUnique({
    where: { email: result.data.email },
  })

  if (existingUser) {
    return {
      values,
      success: false,
      errors: {
        email: ["This email is already registered"],
      },
    }
  }

  return {
    errors: null,
    success: true,
  }
}
```

## エラーの表示

`<FieldError />` でフィールドの横にエラーを表示します。`<Field />` に `data-invalid`、入力に `aria-invalid` を付けてください。

```tsx showLineNumbers
<Field data-invalid={!!formState.errors?.email?.length}>
  <FieldLabel htmlFor="email">Email</FieldLabel>
  <Input
    id="email"
    name="email"
    type="email"
    aria-invalid={!!formState.errors?.email?.length}
  />
  {formState.errors?.email && (
    <FieldError>{formState.errors.email[0]}</FieldError>
  )}
</Field>
```

## フォームのリセット

Server Action でフォームを送信すると、React が自動的にフォーム状態を初期値にリセットします。

### 成功時にリセット

成功時にフォームをリセットするには、Server Action から `values` を岐略すれば、React が自動的に初期値にリセットします。これは標準的な React の振る舞いです。

```tsx showLineNumbers title="actions.ts" {22-26}
export async function demoFormAction(
  _prevState: FormState,
  formData: FormData
) {
  const values = {
    title: formData.get("title") as string,
    description: formData.get("description") as string,
  }

  // Validation.
  if (!result.success) {
    return {
      values,
      success: false,
      errors: result.error.flatten().fieldErrors,
    }
  }

  // Business logic.
  callYourDatabaseOrAPI(values)

  // Omit the values on success to reset the form state.
  return {
    errors: null,
    success: true,
  }
}
```

### 検証エラー時に入力内容を保持

バリデーションエラー時にフォームがリセットされないようにするには、Server Action 内で values を返します。ユーザーの入力内容をフォーム状態に保持するためです。

```tsx showLineNumbers title="actions.ts" {12-17}
export async function demoFormAction(
  _prevState: FormState,
  formData: FormData
) {
  const values = {
    title: formData.get("title") as string,
    description: formData.get("description") as string,
  }

  // Validation.
  if (!result.success) {
    return {
      // Return the values on validation errors.
      values,
      success: false,
      errors: result.error.flatten().fieldErrors,
    }
  }
}
```

## 複雑なフォーム

複数のフィールドと検証を持つ、より複雑なフォームの例。

<ComponentPreview
  name="form-next-complex"
  className="[&_.preview]:h-[1100px] [&_pre]:h-[1100px]!"
  hideCode
/>

### スキーマ

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-complex-schema.ts"
  title="schema.ts"
/>

### フォーム

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-complex.tsx"
  title="form.tsx"
/>

### Server Action

<ComponentSource
  src="/registry/new-york-v4/examples/form-next-complex-action.ts"
  title="actions.ts"
/>
